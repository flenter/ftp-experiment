box: wercker/default
services:
  - wercker/mysql
build:
  steps:
    - add-to-known_hosts:
        host: $WERCKER_MYSQL_HOST
    - script:
        name: login
        code: |
          ssh $WERCKER_MYSQL_HOST
